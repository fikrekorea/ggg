import React from 'react'

import HeroImg from '../images/a31.jpg'

const Hero = (props) => {
  return (
    <>
    <h2>Hero Page</h2>
    {/*
    <div className={props.cName}>   
    <img src={props.cheroImg} alt='heroImg'/>

    <div className='hero-text'>
      <h1>{props.title}</h1>
      <p>{props.text}</p>
      <a href={props.url} className={props.btnClass}>
          {props.buttonText}
      </a>
    </div>
  
    </div>
    */}
    </>
  )
}

export default Hero